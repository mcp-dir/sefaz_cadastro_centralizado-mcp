# Ferramentas

SEFAZ: Cadastro Centralizado de Contribuinte (CCC) expõe 1 ferramenta (todas somente leitura).

### 1. `sefaz_cadastro_centralizado_consultar`
**Input**: `login_cpf` (opcional), `login_senha` (opcional), `pkcs12_cert` (opcional), `pkcs12_pass` (opcional), `cnpj` (opcional), `cpf` (opcional), `ie` (opcional), `uf`

SEFAZ: Cadastro Centralizado de Contribuinte (CCC), consulta em fonte oficial.

## Prompts de exemplo

```
Consultar SEFAZ: Cadastro Centralizado de Contribuinte (CCC)
```
